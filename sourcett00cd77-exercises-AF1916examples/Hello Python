# import the datetime library to access current date and time
import datetime

def my_function(x)
   """purpose of this fuction is to increase the given parameter by one

   Parameters
   -------------
   x : int 
   a numereic value that will be increased by one

   Returns
   ---------
   int 
   increases parameter 'x' by one returns the result from the fuction
   """

   return x + 1


# import the os library to access the current logged in user
import os

# declare a variable named 'user' and set the current logged in user into it
user = os.getlogin()

# declare a variable named 'msg' and set some text into it
msg = f"Jatketaan harjoituksia! {user}"

# print the message into console
print(msg)

# print the current date and time
print(datetime.datetime.now())

# read text from console and echo back
text = input('Kirjoita jotain tekstiä: ')
print("Kirjoitit: ")
print(text)